<% if user_signed_in? and user.followed_by?(current_user) %>
    <%= link_to "Não Seguir", deixar_user_path(user), {:method => :delete, :remote => true, :'data-type' => 'html', :class => "userSeguir seguir botao negative precisaDeLogin"} if user != current_user%>
<% else %>
    <%= link_to "Seguir", seguir_user_path(user), {:remote => true,  :'data-type' => 'html', :class => "userSeguir deixar botao positive precisaDeLogin"} if user != current_user%>
<% end %>