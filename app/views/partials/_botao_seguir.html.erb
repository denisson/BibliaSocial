<% if user_signed_in? and user.followed_by?(current_user) %>
    <%= link_to "Deixar", deixar_user_path(user), {:method => :delete, :remote => true, :'data-type' => 'html', :class => "userSeguir seguir botao negative"} if user != current_user%>
<% else %>
    <%= link_to "Seguir", seguir_user_path(user), {:remote => true,  :'data-type' => 'html', :class => "userSeguir deixar botao positive"} if user != current_user%>
<% end %>